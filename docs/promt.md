В компоненте SearchSheet.tsx добавь под полями ввода список FlatList для подсказок адресов.
Используй expo-location или предложи интеграцию с бесплатным API (например, Photon/OpenStreetMap), чтобы при вводе текста в TextInput появлялись реальные варианты адресов.
При выборе адреса из списка, автоматически обновляй стейты fromCoord/toCoord и fromAddress/toAddress

Надо первым делом изменить "Подъезд" на "Коментарий" и доабвить логику что бы при заказе клиент мог написать Комнатарий типо "Черный забор" и это должно впиысваться рядом с адресом подачи после запятой, также дать логику плюсику, что бы клиент мог добавлять остановку, и эта остановка должна показываться после запятой

Потом дать логику кнопке "Детали" что бы при нажатий появлялась новая шторка, в которой указаны адрес подачи, адрес назначения цена, и еще что нибуть

Тепер логика заказа, после того как клиент все написал и нажал на подтвердить, заказ должен создатся, первым делом оно проверяется есть ли в картах, если есть то таксисту приходит заказ с адресом подачи и рисуется маршрут, если адреса в картах не нашлось, что часто бывает в маленьких городах, ему также приходит заказ но маршрут не рисуется, а пишется текст "Адрес не найден на карте, следуйте тексту или уточните у клиента", заказ должен распределяться честно, по ближайшему таксисту в радисуе 3 километров и также по тому кто больше всех не работал, когда таксист принимает заказ, у клиента должно менятся шторка,